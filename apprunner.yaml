version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - n 18.17.1
    build:      
      -  yarn install
      - yarn build
run: 
  command: yarn start
  network:
    port: 8080
    env: APP_PORT
