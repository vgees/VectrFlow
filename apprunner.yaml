version: 1.0
runtime: nodejs16
build:
  env:
    CI: false
  commands:
    pre-build:
      - n 16.17.0
    build:      
      -  yarn install
      - yarn build
run: 
  command: yarn start
  network:
    port: 8080
    env: APP_PORT
